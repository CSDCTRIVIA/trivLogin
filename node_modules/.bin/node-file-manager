#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../node-file-manager/bin/node-file-manager.sh" "$@"
  ret=$?
else 
  sh  "$basedir/../node-file-manager/bin/node-file-manager.sh" "$@"
  ret=$?
fi
exit $ret
